type Point(a: Number, b: Number) {
    x = a;
    y = b;

    getX() => self.x;
    getY() => self.y;

    setX(x) => self.x := x;
    setY(y) => self.y := y;
};


let pt = new Point(0,1) in
{
    print("x: " @ pt.getX() @ "; y: " @ pt.getY());
};
    
